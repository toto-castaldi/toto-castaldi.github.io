---
phase: 08-sicurezza-dipendenze-dependabot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .github/dependabot.yml
  - .planning/phases/08-sicurezza-dipendenze-dependabot/RESIDUAL-ADVISORIES.md
autonomous: false
requirements: [SEC-01, SEC-02]
must_haves:
  truths:
    - "`npm audit` reports 0 high vulnerabilities (down from 7 high)"
    - "Only the 3 accepted moderate astro advisories remain, each documented with GHSA ID + static-site non-exploitability rationale"
    - "astro stays a single caret-ranged direct dependency (no exact pins, no Astro 6 major bump)"
    - "Dependabot monitors npm + github-actions weekly with grouped PRs"
  artifacts:
    - path: "package-lock.json"
      provides: "Regenerated lockfile with fixed transitive versions (lockfileVersion 3)"
      contains: "\"lockfileVersion\": 3"
    - path: ".github/dependabot.yml"
      provides: "Dependabot v2 governance config (npm + github-actions, weekly, grouped)"
      contains: "package-ecosystem: \"npm\""
    - path: ".planning/phases/08-sicurezza-dipendenze-dependabot/RESIDUAL-ADVISORIES.md"
      provides: "Documented accepted residuals (3 GHSA IDs + rationale)"
      contains: "GHSA-g735-7g2w-hh3f"
  key_links:
    - from: "package.json"
      to: "package-lock.json"
      via: "npm audit fix + npm update regeneration"
      pattern: "astro"
    - from: ".github/dependabot.yml"
      to: ".github/workflows/deploy.yml"
      via: "github-actions ecosystem monitoring"
      pattern: "package-ecosystem: \"github-actions\""
---

<objective>
Eliminate the fixable dependency vulnerabilities in-place (no Astro 6 major bump), document the 3 accepted residual Astro moderate advisories, and add a minimal Dependabot governance config.

Purpose: Satisfies SEC-01 (vulns zeroed or documented+mitigated) and SEC-02 (deps on supported versions, `npm audit` clean or residuals justified). The previous Astro major bump broke the Pages deploy (run 25837277597) — staying within Astro 5.x and fixing only transitives de-risks this.

Output: Regenerated `package-lock.json` (0 high), `.github/dependabot.yml`, `RESIDUAL-ADVISORIES.md` documenting the 3 accepted moderate astro advisories.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-sicurezza-dipendenze-dependabot/08-CONTEXT.md
@.planning/phases/08-sicurezza-dipendenze-dependabot/08-PATTERNS.md
@package.json
@.github/workflows/deploy.yml

<interfaces>
<!-- Current package.json (the ONLY direct dependency, caret-ranged). Preserve this shape per D-04. -->
package.json:
```json
{
  "name": "toto-castaldi.github.io",
  "type": "module",
  "version": "0.0.1",
  "scripts": { "dev": "astro dev", "build": "astro build", "preview": "astro preview", "astro": "astro" },
  "dependencies": { "astro": "^5.17.1" }
}
```

<!-- deploy.yml supplies the .github YAML conventions the new dependabot.yml must mirror: -->
<!-- 2-space indent, lowercase keys, double-quoted string values, branches: ["master"] (NOT main). -->
<!-- withastro/action@v5 reinstalls from the committed package-lock.json — the regenerated lockfile gates the deploy. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix fixable dependency vulnerabilities and regenerate the lockfile</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (current state: single direct dep `astro: ^5.17.1`)
    - .planning/phases/08-sicurezza-dipendenze-dependabot/08-CONTEXT.md (D-01, D-03, D-04)
    - .planning/phases/08-sicurezza-dipendenze-dependabot/08-PATTERNS.md (package.json / package-lock.json sections — preserve caret, lockfileVersion 3, no overrides)
  </read_first>
  <action>
    Capture the baseline first: run `npm audit` and note the counts (expected baseline per CONTEXT: 10 vuln = 7 high, 3 moderate). Then resolve the 7 high + fixable moderate transitives (defu, devalue, h3, picomatch, postcss, rollup, smol-toml, svgo, vite) per D-03 by running `npm audit fix` WITHOUT `--force` (no major bumps), followed by `npm update`. Run `npm install` only if npm reports the lockfile is out of sync. Per D-01, ensure astro resolves to the latest patch >= 5.18.2 within the existing `^5.17.1` caret — the `astro: ^5.17.1` line in package.json must stay caret-ranged and should NOT change to an exact pin (D-04); only package-lock.json should resolve higher. Do NOT add `overrides` or `resolutions` to package.json unless `npm audit fix` (no `--force`) genuinely cannot resolve a transitive without them; if that happens, document why in the SUMMARY. Preserve `"type": "module"`, the 4 scripts, and `"lockfileVersion": 3` in the lockfile. Do NOT touch the `"name": "tmp-astro-scaffold"` scaffold artifact in the lockfile root unless regeneration changes it naturally. After regeneration run `npm audit` again to confirm 0 high remain.
  </action>
  <verify>
    <automated>cd /home/toto/scm-projects/toto-castaldi.github.io && npm audit --omit=dev 2>&1 | grep -iE "high" | grep -ivE "0 high" ; test $? -ne 0 && echo "NO_HIGH_FOUND"</automated>
  </verify>
  <acceptance_criteria>
    - `npm audit` reports 0 high severity vulnerabilities (baseline was 7 high)
    - `npm audit` residuals are exactly the 3 moderate astro advisories (GHSA-g735-7g2w-hh3f, GHSA-j687-52p2-xcff, GHSA-xr5h-phrj-8vxv) and nothing else
    - package.json `dependencies.astro` is still a caret range (matches regex `^\\^5\\.`), NOT an exact version, NOT `^6.`
    - package.json still contains `"type": "module"` and the 4 unchanged scripts (dev, build, preview, astro)
    - package-lock.json contains `"lockfileVersion": 3`
    - No `overrides` or `resolutions` key added to package.json (unless explicitly justified in SUMMARY)
    - astro resolves to >= 5.18.2 in package-lock.json (`grep` the astro resolved version)
  </acceptance_criteria>
  <done>`npm audit` shows 0 high and only the 3 documented moderate astro residuals; package.json unchanged except (possibly) astro caret; package-lock.json regenerated at lockfileVersion 3.</done>
</task>

<task type="auto">
  <name>Task 2: Document accepted residual advisories and add Dependabot governance config</name>
  <files>.planning/phases/08-sicurezza-dipendenze-dependabot/RESIDUAL-ADVISORIES.md, .github/dependabot.yml</files>
  <read_first>
    - .github/workflows/deploy.yml (YAML conventions to mirror: 2-space indent, lowercase keys, double-quoted strings, branches: ["master"])
    - .planning/phases/08-sicurezza-dipendenze-dependabot/08-CONTEXT.md (D-02, D-05, D-06 + Canonical References GHSA links)
    - .planning/phases/08-sicurezza-dipendenze-dependabot/08-PATTERNS.md (dependabot.yml required structure + Dependabot v2 schema)
  </read_first>
  <action>
    Create `.planning/phases/08-sicurezza-dipendenze-dependabot/RESIDUAL-ADVISORIES.md` (lightweight markdown per D-06 — NOT a formal ADR; ADRs are Phase 10). It must list all three accepted moderate astro advisories with their GHSA IDs and advisory URLs and, for each, the static-site non-exploitability rationale: GHSA-g735-7g2w-hh3f (remote allowlist bypass on `matchPathname` — requires SSR routing/middleware; this site is `output: static`, zero-JS, no runtime allowlist), GHSA-j687-52p2-xcff (XSS in `define:vars` with dynamic input — site uses no `define:vars` with untrusted/dynamic input), GHSA-xr5h-phrj-8vxv (server-island encrypted-param replay — site uses no server islands). State explicitly that all three are fixed only by Astro 6 which is out of scope (D-01), that they are accepted residuals satisfying SEC-01 (residuals motivated + mitigated), and record the supply-chain posture of the fix itself (only non-major `npm audit fix` + `npm update`, no new direct deps, lockfile committed as source of truth). Then create `.github/dependabot.yml` per D-05: Dependabot v2 schema (`version: 2`), two `updates` entries — `package-ecosystem: "npm"` (`directory: "/"`) and `package-ecosystem: "github-actions"` (`directory: "/"`) — both with `schedule.interval: "weekly"` and a grouped-updates block per ecosystem (one group each with `patterns: ["*"]`) to reduce PR noise. Mirror deploy.yml conventions: 2-space indentation, lowercase keys, double-quoted string values. Keep it minimal — do NOT add `open-pull-requests-limit`, `reviewers`, `labels`, or `commit-message` knobs (minimal-config principle, D-05). No `target-branch` override is needed since the repo default branch is `master` and Dependabot defaults to it.
  </action>
  <verify>
    <automated>cd /home/toto/scm-projects/toto-castaldi.github.io && grep -q 'package-ecosystem: "npm"' .github/dependabot.yml && grep -q 'package-ecosystem: "github-actions"' .github/dependabot.yml && grep -q 'interval: "weekly"' .github/dependabot.yml && grep -q "GHSA-g735-7g2w-hh3f" .planning/phases/08-sicurezza-dipendenze-dependabot/RESIDUAL-ADVISORIES.md && grep -q "GHSA-j687-52p2-xcff" .planning/phases/08-sicurezza-dipendenze-dependabot/RESIDUAL-ADVISORIES.md && grep -q "GHSA-xr5h-phrj-8vxv" .planning/phases/08-sicurezza-dipendenze-dependabot/RESIDUAL-ADVISORIES.md && echo "ALL_CHECKS_PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/dependabot.yml` starts with `version: 2`
    - `.github/dependabot.yml` contains `package-ecosystem: "npm"` with `directory: "/"`
    - `.github/dependabot.yml` contains `package-ecosystem: "github-actions"` with `directory: "/"`
    - Both ecosystems have `schedule.interval: "weekly"`
    - Both ecosystems have a `groups` block with `patterns: ["*"]` (or equivalent list form)
    - `.github/dependabot.yml` does NOT contain `open-pull-requests-limit`, `reviewers`, `labels`, or `commit-message` keys
    - `RESIDUAL-ADVISORIES.md` contains all three GHSA IDs: GHSA-g735-7g2w-hh3f, GHSA-j687-52p2-xcff, GHSA-xr5h-phrj-8vxv
    - `RESIDUAL-ADVISORIES.md` states each advisory's static-site non-exploitability rationale and references SEC-01
    - `dependabot.yml` uses 2-space indentation and double-quoted string values (matches deploy.yml style)
  </acceptance_criteria>
  <done>RESIDUAL-ADVISORIES.md documents the 3 accepted moderate astro advisories (GHSA IDs + rationale + supply-chain note); .github/dependabot.yml is a minimal valid v2 config monitoring npm + github-actions weekly with grouped updates.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Supply-chain checkpoint — review lockfile diff and audit (T-08-SC)</name>
  <what-built>Regenerated package-lock.json (0 high vulns, 3 accepted moderate astro residuals), RESIDUAL-ADVISORIES.md documenting the residuals, and a minimal .github/dependabot.yml. This is the supply-chain trust-boundary checkpoint (T-08-SC): the lockfile change must be reviewed before it gates the deploy.</what-built>
  <how-to-verify>
    1. Run `cd /home/toto/scm-projects/toto-castaldi.github.io && npm audit` — confirm output shows 0 high and exactly 3 moderate (all astro), matching the expected post-fix state.
    2. Run `git diff package.json` — confirm the only possible change is the astro caret line; `"type": "module"` and the 4 scripts are untouched; no `overrides`/`resolutions` added.
    3. Run `git diff --stat package-lock.json` — confirm the lockfile changed (transitive bumps) and review that no unexpected NEW top-level packages appeared.
    4. Open `.github/dependabot.yml` — confirm 2 ecosystems (npm + github-actions), weekly, grouped, minimal.
    5. Open RESIDUAL-ADVISORIES.md — confirm all 3 GHSA IDs are present with static-site rationale.
  </how-to-verify>
  <resume-signal>Type "approved" if the lockfile diff is trustworthy and audit is clean, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → package-lock.json | Untrusted upstream package versions cross into the build via `npm audit fix` / `npm update` |
| package-lock.json → GitHub Pages build | The committed lockfile is reinstalled 1:1 by `withastro/action@v5` at deploy time |
| Astro runtime features → public site | SSR / `define:vars` / server-island attack surface (not used by this static site) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure / Tampering | astro GHSA-g735-7g2w-hh3f (remote allowlist bypass on `matchPathname`) | accept | Requires SSR routing/middleware; site is `output: static`, zero-JS, no runtime allowlist → not exploitable. Documented in RESIDUAL-ADVISORIES.md per D-02/D-06. |
| T-08-02 | Tampering (XSS) | astro GHSA-j687-52p2-xcff (XSS in `define:vars`) | accept | Site uses no `define:vars` with dynamic/untrusted input → not exploitable. Documented per D-02/D-06. |
| T-08-03 | Spoofing / Replay | astro GHSA-xr5h-phrj-8vxv (server-island encrypted-param replay) | accept | Site uses no server islands → not exploitable. Documented per D-02/D-06. |
| T-08-SC | Tampering (supply chain) | npm transitive updates via `npm audit fix` + `npm update` | mitigate | No `--force` (no major bumps), no new direct deps, lockfile committed as source of truth; blocking human-verify checkpoint (Task 3) reviews the lockfile diff before it gates deploy. |
</threat_model>

<verification>
- `npm audit` post-fix: 0 high, exactly 3 moderate (astro) residuals.
- package.json: single caret astro dependency preserved, no exact pins, no Astro 6.
- package-lock.json: lockfileVersion 3 preserved, transitives bumped.
- `.github/dependabot.yml`: valid Dependabot v2, npm + github-actions, weekly, grouped, minimal.
- RESIDUAL-ADVISORIES.md: 3 GHSA IDs + static-site rationale + SEC-01 reference.
</verification>

<success_criteria>
- SEC-01: high vulnerabilities zeroed; 3 moderate residuals documented with motivation + mitigation.
- SEC-02: deps on supported versions; `npm audit` residuals explicitly justified.
- Dependabot governance in place to prevent recurrence (D-05).
</success_criteria>

<output>
Create `.planning/phases/08-sicurezza-dipendenze-dependabot/08-01-SUMMARY.md` when done. Record: baseline vs post-fix audit counts, the astro resolved version, whether package.json changed, and any `overrides` used (with justification).
</output>
