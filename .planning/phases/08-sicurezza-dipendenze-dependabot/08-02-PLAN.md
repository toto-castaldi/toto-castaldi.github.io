---
phase: 08-sicurezza-dipendenze-dependabot
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified: []
autonomous: false
requirements: [SEC-03, SEC-04]
must_haves:
  truths:
    - "`npm ci` installs cleanly from the regenerated lockfile"
    - "`npm run build` exits 0 (no build regression from the dependency updates)"
    - "Lighthouse reports 100/100/100/100 on all four routes after the updates"
    - "The GitHub Pages deploy run completes successfully after merge (avoiding the prior run 25837277597 failure)"
  artifacts: []
  key_links:
    - from: "package-lock.json"
      to: "withastro/action@v5"
      via: "lockfile reinstall at deploy"
      pattern: "withastro/action@v5"
    - from: "local build (npm run build)"
      to: "GitHub Pages deploy"
      via: "same lockfile, same Astro version"
      pattern: "astro build"
---

<objective>
De-risk the deploy and prove no regression from the Phase 8 dependency updates: clean local install + build, Lighthouse 100×4 on all four routes, and post-merge GitHub Pages deploy confirmation.

Purpose: Satisfies SEC-03 (site builds and Pages deploy succeeds after updates) and SEC-04 (Lighthouse 100/100/100/100 maintained). The previous Astro major bump broke the deploy (run 25837277597) — this plan validates the in-place fix did NOT reintroduce that failure. Since this plan only reads/verifies (no source changes), the lockfile from 08-01 is what gates the deploy via `withastro/action@v5`.

Output: Verification evidence (build exit code, Lighthouse scores ×4, deploy run status) recorded in the SUMMARY. No application files modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-sicurezza-dipendenze-dependabot/08-CONTEXT.md
@.github/workflows/deploy.yml
@.planning/phases/08-sicurezza-dipendenze-dependabot/08-01-SUMMARY.md

<interfaces>
<!-- The four routes that must hit Lighthouse 100 (D-07): -->
<!--   IT home: /     EN home: /en/     Qualifiche IT: /qualifiche     Qualifiche EN: /en/qualifications -->
<!-- Deploy: withastro/action@v5 reinstalls from the committed package-lock.json regenerated in 08-01. -->
<!-- Prior failure to AVOID: GitHub Actions run 25837277597 (major astro bump). This phase does no major bump. -->
<!-- Local env: Node v22.20.0, npm 11.6.2. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean local install and build from the regenerated lockfile</name>
  <files>dist/ (build output, not committed)</files>
  <read_first>
    - .planning/phases/08-sicurezza-dipendenze-dependabot/08-01-SUMMARY.md (what 08-01 changed: astro resolved version, lockfile state)
    - .github/workflows/deploy.yml (confirm withastro/action@v5 reinstalls from lockfile — local `npm ci` mirrors CI install)
    - package.json (build script: `astro build`)
  </read_first>
  <action>
    Reproduce the CI install path locally to mirror `withastro/action@v5`: run `npm ci` (NOT `npm install`) so the install comes strictly from the committed package-lock.json regenerated in 08-01 — this is exactly what the Pages workflow does. If `npm ci` fails because the lockfile is out of sync with package.json, that is a blocking finding from 08-01 and must be reported, not worked around. Then run `npm run build` (`astro build`, output `static`) and confirm it exits 0 with no errors. Note the build output directory (`dist/`) is produced. Per D-07 this is the de-risk step before any push: a clean `npm ci && npm run build` here predicts the deploy outcome because the deploy uses the same lockfile and same Astro 5.x version (no major bump this phase).
  </action>
  <verify>
    <automated>cd /home/toto/scm-projects/toto-castaldi.github.io && npm ci && npm run build && test -d dist && echo "BUILD_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `npm ci` completes with exit code 0 (clean install from lockfile, no "lockfile out of sync" error)
    - `npm run build` completes with exit code 0
    - `dist/` directory is produced by the build
    - Build output includes the four route artifacts (index for `/`, `/en/`, `/qualifiche`, `/en/qualifications`)
  </acceptance_criteria>
  <done>`npm ci && npm run build` both exit 0 from the regenerated lockfile; dist/ is produced — local build mirrors the CI install path with no regression.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Lighthouse 100×4 checkpoint on all four routes</name>
  <what-built>A clean local build from the regenerated lockfile (Task 1). Now Lighthouse must confirm 100×4 is maintained after the dependency updates (SEC-04), reusing the same Lighthouse audit flow from phases v2.0 (Phase 6) / v3.0 (Phase 7).</what-built>
  <how-to-verify>
    1. Serve the freshly built site locally (e.g. `cd /home/toto/scm-projects/toto-castaldi.github.io && npm run preview`).
    2. Run the same Lighthouse audit process used in Phase 6 / Phase 7 against all FOUR routes on the preview server:
       - IT home: `/`
       - EN home: `/en/`
       - Qualifiche IT: `/qualifiche`
       - Qualifiche EN: `/en/qualifications`
    3. Confirm each route reports Performance / Accessibility / Best Practices / SEO = 100/100/100/100.
  </how-to-verify>
  <resume-signal>Type "approved" with the four Lighthouse scores, or describe any route below 100.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: GitHub Pages deploy confirmation (closes run 25837277597 regression)</name>
  <what-built>Local build + Lighthouse 100×4 confirmed. The final step is confirming the GitHub Pages deploy succeeds after merge — directly closing out the prior failure (run 25837277597) referenced in the blockers.</what-built>
  <how-to-verify>
    1. After the Phase 8 changes (regenerated package-lock.json, .github/dependabot.yml) are merged to `master`, the deploy workflow triggers (push to `master`).
    2. Check the latest GitHub Actions "Deploy to GitHub Pages" run: `gh run list --workflow=deploy.yml --limit 3` then `gh run view <run-id>`.
    3. Confirm the run status is `success` (build + deploy jobs green) — `withastro/action@v5` reinstalled from the committed lockfile.
    4. Confirm the live site https://toto-castaldi.github.io/ loads and the four routes are reachable.
  </how-to-verify>
  <resume-signal>Type "approved" with the successful deploy run ID, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed package-lock.json → CI build | `withastro/action@v5` reinstalls dependencies from the lockfile; a broken/incompatible lockfile breaks the public deploy |
| CI deploy → public site | The build artifact is published to GitHub Pages (publicly served) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Denial of Service (availability) | GitHub Pages deploy from regenerated lockfile | mitigate | De-risk per D-07: local `npm ci && npm run build` mirrors the CI install path before push; post-merge deploy run confirmed green; no Astro major bump (the cause of prior failure run 25837277597). |
| T-08-05 | Tampering (regression) | Lighthouse 100×4 after dependency updates | mitigate | Lighthouse re-audit on all four routes (`/`, `/en/`, `/qualifiche`, `/en/qualifications`) before sign-off; blocking checkpoint gates on 100/100/100/100. |
| T-08-SC | Tampering (supply chain) | reinstall from committed lockfile at deploy | mitigate | Lockfile already reviewed in 08-01 (T-08-SC checkpoint); `npm ci` here re-validates it installs deterministically. |
</threat_model>

<verification>
- `npm ci` clean from lockfile, `npm run build` exits 0, dist/ produced.
- Lighthouse 100/100/100/100 on `/`, `/en/`, `/qualifiche`, `/en/qualifications`.
- GitHub Pages deploy run status = success after merge; live site reachable.
</verification>

<success_criteria>
- SEC-03: site builds locally and the GitHub Pages deploy completes successfully (prior failure not reintroduced).
- SEC-04: Lighthouse 100×4 maintained after the dependency updates.
</success_criteria>

<output>
Create `.planning/phases/08-sicurezza-dipendenze-dependabot/08-02-SUMMARY.md` when done. Record: `npm ci` + build exit codes, the four Lighthouse scores, and the successful deploy run ID (contrasted with the prior failure 25837277597).
</output>
