---
phase: 07-pagina-qualifiche-multilingua
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/images/qualifica-pesistica.webp
  - public/assets/images/qualifica-pilates-reformer.webp
  - public/assets/images/qualifica-pilates-cadillac.webp
autonomous: false
requirements: [QUAL-04]
user_setup: []

must_haves:
  truths:
    - "The three diploma preview images exist as redacted WebP files under public/assets/images/"
    - "Third-party autograph signatures and the register number N.39740 are invisible in the published images (burned into pixels, not reversible)"
    - "Antonio Castaldi's name remains visible on all three images"
    - "No original PDF is published anywhere in the repo, dist, or public"
  artifacts:
    - path: "public/assets/images/qualifica-pesistica.webp"
      provides: "Redacted Pesistica diploma preview (portrait)"
    - path: "public/assets/images/qualifica-pilates-reformer.webp"
      provides: "Redacted Pilates Reformer Liv.1 diploma preview (landscape)"
    - path: "public/assets/images/qualifica-pilates-cadillac.webp"
      provides: "Redacted Pilates Cadillac Liv.1 diploma preview (landscape)"
  key_links: []
---

<objective>
Convert the three source diploma PDFs (which live OUTSIDE the repo) into optimized, irreversibly-redacted WebP preview images committed under `public/assets/images/`. This is a one-off local build-tooling task; only the redacted WebPs enter the repo, never the source PDFs.

Purpose: Satisfies QUAL-04 (and D-01, D-02, D-03, D-04, D-05) — the privacy/PII core of the phase. Provides the three image assets that the qualifiche pages (Plan 03) will reference.
Output: Three redacted WebP files plus their final pixel `width`/`height` (record these for Plan 03's `<img>` tags).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-pagina-qualifiche-multilingua/07-CONTEXT.md
@.planning/phases/07-pagina-qualifiche-multilingua/07-RESEARCH.md

<notes>
Source PDFs (OUTSIDE the repo — do NOT copy them in):
- ~/Documents/pt/diploma-pesistica-toto.pdf — Pesistica, MSP Italia, A4 PORTRAIT (595x841 pts)
- ~/Documents/pilates/diploma-toto-pilates-reformer-1.pdf — Pilates Reformer Liv.1, Zen Studio Pilates, A4 LANDSCAPE (841x595 pts)
- ~/Documents/pilates/diploma-toto-pilates-cadillac-1.pdf — Pilates Cadillac Liv.1, Zen Studio Pilates, A4 LANDSCAPE (841x595 pts)

What to redact (user-locked, verified WITH the user — not assumptions):
- Pesistica: signatures of Alessandra Caligaris AND Prof. Gian Francesco Lupattelli, AND the register number N.39740 (bottom-right).
- Reformer: signature of Cristian Campana (single signature, no register number).
- Cadillac: signature of Cristian Campana (single signature, no register number).
- KEEP VISIBLE: "Antonio Castaldi" on all three (D-03). Printed names under the signatures may remain — cover only the signature scribble (D-01).

Tooling verified present (no installs): pdftoppm 24.02.0, convert (ImageMagick 6.9.12, libwebp 1.3.2), gs 10.02.1, pdfinfo 24.02.0. `magick`/`cwebp` are absent — use `convert`.

The exact pixel coordinates of each redaction region are NOT pre-computed — they MUST be derived by rendering each PDF, opening the raster, and reading the pixel coordinates of each region (orientation differs per diploma). See RESEARCH.md "Code Examples" (pdftoppm → convert -draw → WebP) and Pitfall 4 (wrong coordinates / reversible redaction).
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rasterize the three PDFs and identify redaction coordinates</name>
  <files>/tmp/pesistica-raw.png, /tmp/pilates-reformer-raw.png, /tmp/pilates-cadillac-raw.png</files>
  <read_first>
    - .planning/phases/07-pagina-qualifiche-multilingua/07-RESEARCH.md ("Code Examples" lines 287-321; "Common Pitfalls" Pitfall 2 and Pitfall 4)
    - .planning/phases/07-pagina-qualifiche-multilingua/07-CONTEXT.md (D-01..D-05)
  </read_first>
  <action>
    Confirm each source PDF's orientation with `pdfinfo` (Pesistica portrait; both Pilates landscape). Rasterize each PDF to a temporary PNG with `pdftoppm -png -r 200 -singlefile <pdf> /tmp/<slug>-raw` (200 DPI gives a legible scan at ~1000px published width while keeping each WebP under ~150KB per Pitfall 2; tune DPI later only if text is illegible or file too large). Run `identify /tmp/<slug>-raw.png` to record exact pixel dimensions. Then OPEN each raw PNG (use the Read tool to view it) and read off the pixel coordinates (x1,y1 x2,y2) of every region to redact: for Pesistica the two signatures (Alessandra Caligaris, Prof. Gian Francesco Lupattelli) AND the N.39740 register number bottom-right; for each Pilates the single Cristian Campana signature. Verify the regions do NOT overlap "Antonio Castaldi" (D-03) or the body/date text — if a signature region touches the visible name, tighten the rectangle. Record all coordinates per file for Task 2. Do NOT copy any PDF into the repo.
  </action>
  <verify>
    <automated>for p in ~/Documents/pt/diploma-pesistica-toto.pdf ~/Documents/pilates/diploma-toto-pilates-reformer-1.pdf ~/Documents/pilates/diploma-toto-pilates-cadillac-1.pdf; do pdfinfo "$p" >/dev/null || exit 1; done; test -f /tmp/pesistica-raw.png && test -f /tmp/pilates-reformer-raw.png && test -f /tmp/pilates-cadillac-raw.png</automated>
  </verify>
  <done>Three raw PNGs exist in /tmp; pixel dimensions recorded via identify; redaction coordinates (signatures + N.39740) recorded per diploma; "Antonio Castaldi" confirmed outside every redaction region.</done>
</task>

<task type="auto">
  <name>Task 2: Burn opaque redaction rectangles and export optimized WebP</name>
  <files>public/assets/images/qualifica-pesistica.webp, public/assets/images/qualifica-pilates-reformer.webp, public/assets/images/qualifica-pilates-cadillac.webp</files>
  <read_first>
    - .planning/phases/07-pagina-qualifiche-multilingua/07-RESEARCH.md ("Code Examples" lines 302-321; Anti-Patterns: no blur, no CSS-only redaction)
    - .planning/phases/07-pagina-qualifiche-multilingua/07-PATTERNS.md (asset section: location convention public/assets/images/, naming qualifica-<slug>.webp, ~150KB budget)
  </read_first>
  <action>
    For each raw PNG, burn the redaction into pixels with `convert <raw>.png -fill '#1a1a1a' -draw "rectangle X1,Y1 X2,Y2" [...one -draw per region...] /tmp/<slug>-redacted.png` using the coordinates from Task 1 (fill `#1a1a1a` matches the site's dark text token for theme coherence; opaque, NOT blur — D-04). Pesistica gets three -draw rectangles (two signatures + N.39740); each Pilates gets one. Then resize and export optimized WebP stripped of metadata: `convert /tmp/<slug>-redacted.png -resize 1000x -strip -quality 80 -define webp:method=6 public/assets/images/qualifica-<slug>.webp` for slugs `pesistica`, `pilates-reformer`, `pilates-cadillac`. The `-strip` removes EXIF/source metadata (Information Disclosure mitigation). Run `identify public/assets/images/qualifica-*.webp` and record the FINAL width/height of each WebP — Plan 03 needs these exact values for the `<img>` width/height attributes (prevents CLS / protects Performance 100). Confirm each WebP is under ~150KB.
  </action>
  <verify>
    <automated>cd /home/toto/scm-projects/toto-castaldi.github.io && test -f public/assets/images/qualifica-pesistica.webp && test -f public/assets/images/qualifica-pilates-reformer.webp && test -f public/assets/images/qualifica-pilates-cadillac.webp && for f in public/assets/images/qualifica-*.webp; do sz=$(stat -c%s "$f"); test "$sz" -lt 153600 || { echo "$f too big: $sz"; exit 1; }; done && ! find . public -name '*.pdf' | grep -q .</automated>
  </verify>
  <done>Three WebP files exist under public/assets/images/, each under ~150KB, with metadata stripped; no PDF anywhere in the repo; final width/height of each WebP recorded for Plan 03.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Human-verify redaction is irreversible and complete</name>
  <files>public/assets/images/qualifica-pesistica.webp, public/assets/images/qualifica-pilates-reformer.webp, public/assets/images/qualifica-pilates-cadillac.webp</files>
  <action>Pause for the user to visually confirm the redaction. No code changes — verification only.</action>
  <what-built>Three redacted WebP diploma previews under public/assets/images/ (qualifica-pesistica.webp, qualifica-pilates-reformer.webp, qualifica-pilates-cadillac.webp), with third-party signatures and the N.39740 register number burned over with opaque rectangles.</what-built>
  <how-to-verify>
    1. Open each of the three WebP files in an image viewer (or via the Read tool to view them inline).
    2. Pesistica: confirm BOTH signatures (Alessandra Caligaris, Prof. Gian Francesco Lupattelli) are fully covered AND the register number N.39740 (bottom-right) is fully covered — no faint scribble or digit visible at the rectangle edges.
    3. Reformer and Cadillac: confirm the Cristian Campana signature is fully covered on each.
    4. On all three: confirm "Antonio Castaldi" is STILL clearly visible (D-03), and the title/body/date text is legible.
    5. Confirm the redaction is opaque (solid fill), NOT a blur, and that nothing of the original signature shows through.
  </how-to-verify>
  <verify><human-check>All third-party signatures + N.39740 fully and irreversibly covered; Antonio Castaldi visible.</human-check></verify>
  <done>User confirms every signature and the register number are fully and irreversibly covered, and Antonio Castaldi remains visible.</done>
  <resume-signal>Type "approved" once all signatures + N.39740 are fully and irreversibly covered and Antonio Castaldi remains visible, or describe which region is still exposed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source PDF (private, ~/Documents) → published WebP (public, GitHub Pages) | Third-party PII (signatures, register number) crosses from a private document into a world-readable static asset. This is the only trust boundary in the phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | redaction of signatures + N.39740 in qualifica-*.webp | mitigate | Burn opaque `#1a1a1a` rectangles into source pixels via `convert -draw` (D-04); human checkpoint visually confirms no region shows through; NOT blur, NOT CSS overlay. |
| T-07-02 | Information Disclosure | source PDFs accidentally committed | mitigate | PDFs stay in ~/Documents (outside repo); pipeline reads from there and writes only WebP; `! find . public -name '*.pdf'` asserts none committed (QUAL-04). |
| T-07-03 | Information Disclosure | EXIF/source metadata leaked in exported WebP | mitigate | `convert -strip` removes all metadata during export. |
| — | Spoofing / Tampering / Repudiation / DoS / Elevation | N/A | accept | Static, no-backend, no-auth, no-input site; no package installs in this plan. These STRIDE categories do not apply (see RESEARCH Security Domain). |
</threat_model>

<verification>
- `pdfinfo` succeeds on all three source PDFs; three raw PNGs produced in /tmp.
- Three `qualifica-*.webp` exist under public/assets/images/, each < ~150KB, metadata stripped.
- `! find . public -name '*.pdf'` — no PDF in repo or public.
- Human checkpoint: signatures + N.39740 fully and irreversibly covered; "Antonio Castaldi" visible.
- Final pixel width/height of each WebP recorded for Plan 03.
</verification>

<success_criteria>
Three redacted, optimized WebP diploma previews committed under public/assets/images/; all third-party signatures and the register number N.39740 irreversibly covered; Antonio Castaldi visible; no original PDF published; final image dimensions recorded.
</success_criteria>

<output>
Create `.planning/phases/07-pagina-qualifiche-multilingua/07-01-SUMMARY.md` when done. Record the final `width`/`height` (in px) of each of the three WebP files — Plan 03 consumes these exact values.
</output>
