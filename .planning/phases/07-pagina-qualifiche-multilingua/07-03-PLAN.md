---
phase: 07-pagina-qualifiche-multilingua
plan: 03
type: execute
wave: 2
depends_on: [07-01, 07-02]
files_modified:
  - src/pages/qualifiche.astro
  - src/pages/en/qualifications.astro
  - src/styles/global.css
autonomous: false
requirements: [QUAL-01, QUAL-02, QUAL-03, QUAL-05, QUAL-06, INT-01, INT-03, INT-04]
user_setup: []

must_haves:
  truths:
    - "User can visit /qualifiche (IT) and /en/qualifications (EN) and see the three qualifications stacked vertically, each with a preview image, label, body+date, and descriptive alt-text"
    - "Each page reuses Base.astro and Section.astro, supports dark/light mode, and gets all text from the i18n dictionary"
    - "Each page declares reciprocal hreflang to its counterpart and a back-to-home text link"
    - "Each preview can be zoomed via a keyboard-accessible mechanism (native <dialog> + Invoker, or an <a href> fallback) with no degradation to Lighthouse 100/100/100/100"
  artifacts:
    - path: "src/pages/qualifiche.astro"
      provides: "IT qualifiche page at /qualifiche"
      contains: "qualifiche.title"
    - path: "src/pages/en/qualifications.astro"
      provides: "EN qualifications page at /en/qualifications"
      contains: "qualifiche.title"
    - path: "src/styles/global.css"
      provides: "figure/img and (if used) dialog/::backdrop rules; images max-width:100% inside 65ch"
  key_links:
    - from: "src/pages/qualifiche.astro"
      to: "/assets/images/qualifica-pesistica.webp"
      via: "<img src>"
      pattern: "qualifica-pesistica.webp"
    - from: "src/pages/qualifiche.astro"
      to: "Base.astro alternates prop"
      via: "alternates={{ it: '/qualifiche', en: '/en/qualifications' }}"
      pattern: "alternates"
---

<objective>
Build the two qualifiche pages (IT `/qualifiche`, EN `/en/qualifications`), each reusing `Base.astro` + `Section.astro`, stacking the three diploma previews vertically with label/body/date and descriptive alt-text, an accessible zoom, reciprocal hreflang via the `alternates` prop, and a back-to-home link. Add the net-new figure/img (and dialog) CSS to `global.css`.

Purpose: Delivers the user-facing pages — QUAL-01, QUAL-02, QUAL-03, QUAL-05, QUAL-06, completes INT-01/INT-03, and must hold Lighthouse 100/100/100/100 (INT-04).
Output: Two new pages + CSS additions. Builds to dist/qualifiche/index.html and dist/en/qualifications/index.html.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-pagina-qualifiche-multilingua/07-CONTEXT.md
@.planning/phases/07-pagina-qualifiche-multilingua/07-RESEARCH.md
@.planning/phases/07-pagina-qualifiche-multilingua/07-PATTERNS.md
@.planning/phases/07-pagina-qualifiche-multilingua/07-01-SUMMARY.md
@.planning/phases/07-pagina-qualifiche-multilingua/07-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 (already merged): Base.astro now accepts alternates?: { it: string; en: string } -->
Pass `alternates={{ it: '/qualifiche', en: '/en/qualifications' }}` on BOTH pages (same object works on both — Base computes both hreflang links and the switcher from it).

<!-- i18n keys available after Plan 02 (use via useTranslations) -->
qualifiche.title, qualifiche.description, qualifiche.intro, qualifiche.back,
qualifiche.{pesistica,reformer,cadillac}.label, qualifiche.{pesistica,reformer,cadillac}.body,
qualifiche.alt.{pesistica,reformer,cadillac}, qualifiche.zoom.{pesistica,reformer,cadillac}, qualifiche.close

<!-- From Plan 01 SUMMARY: the three WebP files and their final pixel width/height -->
public/assets/images/qualifica-pesistica.webp (portrait), qualifica-pilates-reformer.webp (landscape), qualifica-pilates-cadillac.webp (landscape).
Use the EXACT width/height recorded in 07-01-SUMMARY.md for each <img> (prevents CLS / protects Performance 100).

<!-- Page skeleton pattern (src/pages/index.astro) -->
IT page imports: `../layouts/Base.astro`, `../components/Section.astro`, `{ useTranslations } from '../i18n/utils'`, `const t = useTranslations('it')`.
EN page (src/pages/en/qualifications.astro) imports with `../../` depth and `useTranslations('en')`.
Rich strings render with `<p set:html={t(...)} />`; plain strings with `<p>{t(...)}</p>`.

<!-- Layout (src/styles/global.css) -->
Global 65ch column on <body>; there are currently ZERO img/figure/dialog rules — these are net-new additions. New images must be max-width:100% to stay inside the column.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the IT qualifiche page with previews, zoom, and back-home link</name>
  <files>src/pages/qualifiche.astro</files>
  <read_first>
    - src/pages/index.astro (whole file — skeleton analog)
    - src/components/Section.astro (whole file)
    - .planning/phases/07-pagina-qualifiche-multilingua/07-01-SUMMARY.md (exact image width/height)
    - .planning/phases/07-pagina-qualifiche-multilingua/07-RESEARCH.md (Pattern 2 native <dialog>+Invoker lines 193-224; Pattern 3 <a href> fallback lines 226-236; Pitfall 2 image weight; Open Question 1 backdrop-close)
    - .planning/phases/07-pagina-qualifiche-multilingua/07-CONTEXT.md (D-06 vertical stack, D-07 zoom + a11y, D-08/D-11 card content, D-13 back link, D-14 intro)
  </read_first>
  <action>
    Create `src/pages/qualifiche.astro` mirroring `index.astro`: import Base, Section, useTranslations; `const t = useTranslations('it')`. Wrap in `<Base title={t('qualifiche.title')} description={t('qualifiche.description')} alternates={{ it: '/qualifiche', en: '/en/qualifications' }}>`. Body: `<main>` with `<h1>{t('qualifiche.title')}</h1>`, a single intro `<p>{t('qualifiche.intro')}</p>` (D-14), a back-home text link `<a href="/">{t('qualifiche.back')}</a>` near the top (D-13), then one `<Section>` per diploma (D-06 vertical stack, full width) each containing a `<figure>` with the preview `<img src="/assets/images/qualifica-<slug>.webp" width="W" height="H" loading="lazy" decoding="async" alt={t('qualifiche.alt.<slug>')} />` (W/H from 07-01-SUMMARY) and a `<figcaption>` showing `{t('qualifiche.<slug>.label')}` + `{t('qualifiche.<slug>.body')}` (D-08/D-11). For zoom (D-07), use RESEARCH Pattern 2: wrap the thumbnail in a `<button command="show-modal" commandfor="dlg-<slug>" aria-label={t('qualifiche.zoom.<slug>')}>` and add a sibling `<dialog id="dlg-<slug>">` containing a close `<button command="close" commandfor="dlg-<slug>" aria-label={t('qualifiche.close')}>` and the full image. Ship ESC + visible-close-button only (zero author JS, fully accessible — Open Question 1 recommendation); do NOT add a CSS-only :target/checkbox modal (Pitfall 1). ACCEPTANCE FLOOR: if the Lighthouse Accessibility/Best-Practices score dips below 100 with the dialog, fall back to RESEARCH Pattern 3 — make each preview a plain `<a href="/assets/images/qualifica-<slug>.webp">` wrapping the `<img>` (zero a11y risk, works everywhere). Document which approach shipped in the SUMMARY. Slugs: `pesistica`, `pilates-reformer`→key prefix `reformer`, `pilates-cadillac`→key prefix `cadillac`.
  </action>
  <verify>
    <automated>cd /home/toto/scm-projects/toto-castaldi.github.io && npm run build && test -f dist/qualifiche/index.html && [ "$(grep -c 'qualifica-' dist/qualifiche/index.html)" -ge 3 ] && grep -o 'alt="[^"]*"' dist/qualifiche/index.html | grep -q . && grep -q 'href="/"' dist/qualifiche/index.html && grep 'hreflang="en"' dist/qualifiche/index.html | grep -q 'en/qualifications'</automated>
  </verify>
  <done>dist/qualifiche/index.html exists with ≥3 qualifica- image refs, non-empty descriptive alt-text, a back-home link, and reciprocal hreflang to /en/qualifications; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Build the EN qualifications page and add figure/dialog CSS</name>
  <files>src/pages/en/qualifications.astro, src/styles/global.css</files>
  <read_first>
    - src/pages/en/index.astro (whole file — EN skeleton analog, ../../ import depth)
    - src/pages/qualifiche.astro (the IT sibling from Task 1)
    - src/styles/global.css (lines 33, 64-75 — container width / body rules)
    - .planning/phases/07-pagina-qualifiche-multilingua/07-RESEARCH.md (Pattern 2 ::backdrop styling; Pitfall 2)
    - .planning/phases/07-pagina-qualifiche-multilingua/07-PATTERNS.md (layout/container width lines 242-249; "No Analog Found" lines 261-270)
  </read_first>
  <action>
    Create `src/pages/en/qualifications.astro` as a structural clone of the IT page from Task 1, differing only in: `../../` import depth, `const t = useTranslations('en')`, and the back-home link target `<a href="/en/">{t('qualifiche.back')}</a>`. Keep the SAME `alternates={{ it: '/qualifiche', en: '/en/qualifications' }}` object. Same three Sections, same image src/width/height, same zoom approach as Task 1 (mirror whatever Task 1 shipped — dialog or `<a href>` fallback). Then add the net-new CSS to `src/styles/global.css` (there are currently zero img/figure/dialog rules): `figure { margin: 0 }`, `figure img { max-width: 100%; height: auto; display: block; border: 1px solid var(--color-border) }` (max-width:100% keeps images inside the 65ch column), a borderless full-width zoom-trigger button style (`button.zoom-trigger { border: 0; padding: 0; background: none; cursor: zoom-in; width: 100% }`), and — only if the dialog approach shipped — `dialog.zoom-dialog` rules (max sizing, centered) plus `dialog::backdrop { background: rgb(0 0 0 / 0.7) }` for the dim overlay. Use the existing CSS custom properties (--color-border etc.) so dark/light mode works automatically (QUAL-05). Do not introduce render-blocking assets.
  </action>
  <verify>
    <automated>cd /home/toto/scm-projects/toto-castaldi.github.io && npm run build && test -f dist/en/qualifications/index.html && [ "$(grep -c 'qualifica-' dist/en/qualifications/index.html)" -ge 3 ] && grep 'hreflang="it"' dist/en/qualifications/index.html | grep -q '/qualifiche' && grep 'hreflang="en"' dist/index.html | grep -q 'github.io/en/' && ! find dist public -name '*.pdf' | grep -q .</automated>
  </verify>
  <done>dist/en/qualifications/index.html exists with ≥3 image refs and reciprocal hreflang to /qualifiche; home-page hreflang still points to roots (no regression); figure/img/dialog CSS added; no PDF in dist/public; build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify pages, zoom a11y, dark/light, and Lighthouse 100x4</name>
  <files>src/pages/qualifiche.astro, src/pages/en/qualifications.astro</files>
  <action>Pause for the user to verify the built pages in a browser. No code changes unless Lighthouse dips, in which case switch the zoom to the <a href> fallback (Pattern 3) and re-audit.</action>
  <what-built>Two qualifiche pages (/qualifiche IT, /en/qualifications EN) reusing Base/Section, with three vertically-stacked redacted diploma previews, labels/body/date, descriptive alt-text, an accessible zoom (native dialog or <a href> fallback), reciprocal hreflang, and a back-to-home link. New figure/img/dialog CSS in global.css.</what-built>
  <how-to-verify>
    1. Run `npm run build && npm run preview`.
    2. Visit http://localhost:4321/qualifiche and http://localhost:4321/en/qualifications — confirm the three qualifications render stacked vertically with image + label + body/date, and the page looks consistent with the home (65ch, typography).
    3. Toggle dark/light on both routes — confirm previews and zoom overlay are legible in both (QUAL-05).
    4. Test the zoom: open a preview, press ESC to close, confirm focus returns to the trigger and is trapped while open (D-07 keyboard a11y). If the <a href> fallback shipped, confirm clicking opens the full image and browser zoom works.
    5. Click the linkified Fitness text on the home page (/ and /en/) — confirm it lands on the correct-language qualifiche page (INT-01).
    6. Run Lighthouse on BOTH /qualifiche and /en/qualifications in light AND dark — confirm 100/100/100/100 on all four categories (INT-04). If Accessibility or Best-Practices is below 100 with the dialog, switch to the <a href> fallback (Pattern 3) and re-audit.
  </how-to-verify>
  <verify><human-check>Both pages render in light/dark; zoom keyboard-accessible (ESC + focus return); Fitness links navigate correctly; Lighthouse 100/100/100/100 on both routes.</human-check></verify>
  <done>User confirms both pages render correctly in light/dark, zoom is keyboard-accessible, Fitness links work, and Lighthouse is 100/100/100/100 on both routes.</done>
  <resume-signal>Type "approved" once both pages render correctly in light/dark, the zoom is keyboard-accessible, the Fitness links work, and Lighthouse is 100/100/100/100 on both routes — or describe the failing metric.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published redacted WebP → browser (public) | The previews are world-readable static assets. They were already irreversibly redacted in Plan 01; this plan only references them — it must NOT re-expose the source via a PDF link or downloadable original. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Information Disclosure | linking to an original PDF instead of the redacted WebP | mitigate | Pages reference only `/assets/images/qualifica-*.webp`; the zoom links to the redacted WebP, never a PDF; `! find dist public -name '*.pdf'` asserts none published (QUAL-04, Task 2 verify). |
| T-07-07 | Tampering (XSS) | set:html in qualifiche strings | accept | Strings are author-controlled dictionary entries; no user input on this static site (RESEARCH Security Domain). |
| — | PII redaction | inherited | accept | Redaction completeness is verified in Plan 01; this plan does not alter image pixels. |
| — | Spoofing / Repudiation / DoS / Elevation | N/A | accept | Static, no-backend, no-auth, no-input site; no package installs. Not applicable. |
</threat_model>

<verification>
- `npm run build` produces dist/qualifiche/index.html and dist/en/qualifications/index.html.
- `grep -c 'qualifica-' dist/qualifiche/index.html` ≥ 3 (QUAL-03); same for EN.
- `grep -o 'alt="[^"]*"' dist/qualifiche/index.html` non-empty/descriptive (QUAL-06).
- hreflang reciprocity (INT-03): /qualifiche → en/qualifications; /en/qualifications → /qualifiche; home pages still → roots.
- `! find dist public -name '*.pdf'` (QUAL-04 floor).
- Human checkpoint: dark/light parity (QUAL-05), keyboard-accessible zoom (D-07), Fitness link navigation (INT-01), Lighthouse 100/100/100/100 on both routes (INT-04).
</verification>

<success_criteria>
Both qualifiche pages render the three redacted previews with labels/body/date and descriptive alt-text, reuse Base/Section with dark/light support, declare reciprocal hreflang, link back to the locale home, expose a keyboard-accessible zoom, and hold Lighthouse 100/100/100/100. No PDF published.
</success_criteria>

<output>
Create `.planning/phases/07-pagina-qualifiche-multilingua/07-03-SUMMARY.md` when done. Record which zoom approach shipped (native dialog vs <a href> fallback) and the final Lighthouse scores for both routes.
</output>
