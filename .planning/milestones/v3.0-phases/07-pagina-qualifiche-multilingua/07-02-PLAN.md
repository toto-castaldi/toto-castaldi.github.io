---
phase: 07-pagina-qualifiche-multilingua
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/Base.astro
  - src/i18n/ui.ts
  - src/pages/index.astro
  - src/pages/en/index.astro
autonomous: true
requirements: [INT-01, INT-02, INT-03]
user_setup: []

must_haves:
  truths:
    - "Base.astro accepts an optional alternates prop and emits path-aware hreflang/canonical from it, defaulting to locale roots so the home pages stay byte-identical"
    - "The body language switcher on a page with alternates set links to the other language's counterpart page, not the home root"
    - "The i18n dictionary contains all qualifiche.* keys in both it and en blocks"
    - "The Fitness paragraph (section.fitness.p2) is an internal link to the qualifiche page in both IT and EN, and both home pages render it via set:html"
  artifacts:
    - path: "src/layouts/Base.astro"
      provides: "Path-aware hreflang/canonical via optional alternates prop + alternates-driven language switcher"
      contains: "alternates"
    - path: "src/i18n/ui.ts"
      provides: "qualifiche.* dictionary keys (IT+EN) and linkified section.fitness.p2"
      contains: "qualifiche.title"
    - path: "src/pages/index.astro"
      provides: "Fitness p2 rendered via set:html (embeds the qualifiche link)"
    - path: "src/pages/en/index.astro"
      provides: "Fitness p2 rendered via set:html (embeds the qualifiche link)"
  key_links:
    - from: "src/pages/index.astro"
      to: "/qualifiche"
      via: "internal <a> inside section.fitness.p2 rendered with set:html"
      pattern: "href=\"/qualifiche\""
    - from: "src/pages/en/index.astro"
      to: "/en/qualifications"
      via: "internal <a> inside section.fitness.p2 rendered with set:html"
      pattern: "href=\"/en/qualifications\""
---

<objective>
Extend the shared foundation so the new qualifiche pages can plug in: make `Base.astro` path-aware for hreflang/canonical via an optional `alternates` prop (defaulting to locale roots so the home pages do NOT regress), add all `qualifiche.*` i18n keys in both IT and EN, and linkify the Fitness paragraph so the home pages link into the qualifiche page.

Purpose: Satisfies INT-02 (all strings via dictionary), INT-03 (reciprocal hreflang for the new routes), and INT-01 (Fitness-section link into qualifiche). This plan touches NO page that depends on the diploma images, so it runs in Wave 1 in parallel with the image redaction.
Output: Modified Base.astro, ui.ts, index.astro, en/index.astro. Site still builds; home-page hreflang unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-pagina-qualifiche-multilingua/07-CONTEXT.md
@.planning/phases/07-pagina-qualifiche-multilingua/07-RESEARCH.md
@.planning/phases/07-pagina-qualifiche-multilingua/07-PATTERNS.md

<interfaces>
<!-- Current Base.astro Props + hreflang derivation (src/layouts/Base.astro, VERIFIED) -->
Props (lines 2-6): `interface Props { title: string; description?: string }`
Current hreflang vars (lines 16-17): `const itUrl = getAbsoluteLocaleUrl('it','')` / `const enUrl = getAbsoluteLocaleUrl('en','')` — both resolve to locale ROOTS (https://toto-castaldi.github.io/ and .../en/).
Language switcher (lines 22-24, anchor lines 94-101): `const alternateUrl = getRelativeLocaleUrl(alternateLang, '')` — also points to root.
hreflang <link> emission (lines 86-88): it=itUrl, en=enUrl, x-default=itUrl.
Canonical (line 7): `new URL(Astro.url.pathname, Astro.site)` — already per-page correct, DO NOT TOUCH.

<!-- i18n dictionary (src/i18n/ui.ts, VERIFIED) -->
Shape: `export const ui = { it: {...}, en: {...} } as const;` flat dot-keyed strings. Keys MUST exist in BOTH it and en (utils.ts types t off keyof (typeof ui)['it'] → TS build fails on asymmetry — this is the INT-02 guardrail).
Embedded-link convention (section.cs.p2): external links use `target="_blank" rel="noopener noreferrer"`; INTERNAL links omit target="_blank".
Current section.fitness.p2 IT: 'Lavoro come Personal Trainer e come istruttore di Pilates in due palestre dell'interland milanese.'
Current section.fitness.p2 EN: 'I work as a Personal Trainer and Pilates instructor at two gyms in the Milan metropolitan area.'

<!-- Home pages render fitness p2 as plain text today (src/pages/index.astro line 46, src/pages/en/index.astro line 29) -->
`<p>{t('section.fitness.p2')}</p>` — must become `<p set:html={t('section.fitness.p2')} />` (pattern already used for entrepreneurship.p1 / cs.p2).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make Base.astro path-aware via an optional alternates prop</name>
  <files>src/layouts/Base.astro</files>
  <read_first>
    - src/layouts/Base.astro (whole file)
    - .planning/phases/07-pagina-qualifiche-multilingua/07-RESEARCH.md (Pattern 1, lines 166-191; Pitfall 3; Assumption A1)
    - .planning/phases/07-pagina-qualifiche-multilingua/07-PATTERNS.md (Base.astro section lines 95-136)
  </read_first>
  <action>
    Extend the `Props` interface with an optional `alternates?: { it: string; en: string }` and destructure it from `Astro.props`. Replace the hardcoded `itUrl`/`enUrl` so they become path-aware: `itUrl = new URL(alternates?.it ?? '/', Astro.site).toString()` and `enUrl = new URL(alternates?.en ?? '/en/', Astro.site).toString()`. The defaults (`'/'`, `'/en/'`) MUST reproduce today's `getAbsoluteLocaleUrl('it','')` / `('en','')` output so home pages stay byte-identical (Assumption A1 — verify in Task 3). Drive the body language switcher from the same prop: when `alternates` is set, the switcher's `alternateUrl` should point to the OTHER language's counterpart (`alternateLang === 'en' ? alternates.en : alternates.it`) instead of the locale root; when `alternates` is absent, keep the current `getRelativeLocaleUrl(alternateLang, '')` behavior (Open Question 2 recommendation, RESEARCH line 191). Leave `x-default` following `itUrl` (becomes path-aware automatically). DO NOT touch the canonical line (line 7) — it already uses `Astro.url.pathname` and is per-page correct. Keep `getAbsoluteLocaleUrl`/`getRelativeLocaleUrl` imports if still referenced; remove only if fully unused.
  </action>
  <verify>
    <automated>cd /home/toto/scm-projects/toto-castaldi.github.io && grep -q 'alternates' src/layouts/Base.astro && npm run build && grep 'hreflang="en"' dist/index.html | grep -q 'github.io/en/' && grep 'hreflang="it"' dist/index.html | grep -q 'github.io/'</automated>
  </verify>
  <done>Base.astro has an optional `alternates` prop driving itUrl/enUrl and the switcher; build passes; home-page hreflang still points to locale roots (no regression).</done>
</task>

<task type="auto">
  <name>Task 2: Add qualifiche.* dictionary keys and linkify the Fitness paragraph</name>
  <files>src/i18n/ui.ts</files>
  <read_first>
    - src/i18n/ui.ts (whole file)
    - .planning/phases/07-pagina-qualifiche-multilingua/07-RESEARCH.md ("New i18n keys to add" lines 336-355; "linkify the Fitness paragraph" lines 323-333)
    - .planning/phases/07-pagina-qualifiche-multilingua/07-CONTEXT.md (D-08..D-15)
  </read_first>
  <action>
    In `src/i18n/ui.ts`, add these keys to BOTH the `it` and `en` blocks (asymmetry breaks the TS build — INT-02 guardrail): `qualifiche.title` (IT "Qualifiche" / EN "Qualifications"), `qualifiche.description` (a concise SEO meta description per locale), `qualifiche.intro` (IT "Le mie certificazioni come Personal Trainer e istruttore di Pilates." / EN "My certifications as a Personal Trainer and Pilates instructor." — D-14), `qualifiche.back` (IT "← Torna alla home" / EN "← Back to home" — D-13), `qualifiche.pesistica.label` (IT "Personal Trainer" / EN "Certified Fitness Trainer" — D-09), `qualifiche.pesistica.body` ("MSP Italia — 05/12/2025" — D-11), `qualifiche.reformer.label` (IT "Pilates Reformer Liv.1" / EN "Pilates Reformer Level 1" — D-10), `qualifiche.reformer.body` ("Zen Studio Pilates — 16/11/2025"), `qualifiche.cadillac.label` (IT "Pilates Cadillac Liv.1" / EN "Pilates Cadillac Level 1"), `qualifiche.cadillac.body` ("Zen Studio Pilates — 21/02/2026"), and descriptive WCAG-AA alt-text keys `qualifiche.alt.pesistica` / `qualifiche.alt.reformer` / `qualifiche.alt.cadillac` (each describing the diploma's role/body/level in the locale, NOT just "diploma" — QUAL-06). Also add zoom-trigger and close labels `qualifiche.zoom.pesistica` / `qualifiche.zoom.reformer` / `qualifiche.zoom.cadillac` (IT "Ingrandisci il diploma …" / EN "Enlarge the … diploma") and `qualifiche.close` (IT "Chiudi" / EN "Close") — Plan 03 uses these for the dialog. Then linkify `section.fitness.p2`: wrap the role phrase in an INTERNAL anchor — IT `<a href="/qualifiche">Personal Trainer e come istruttore di Pilates</a>`, EN `<a href="/en/qualifications">Personal Trainer and Pilates instructor</a>` (omit target="_blank" — same-site nav per PATTERNS line 151). Use descriptive link text (D-12).
  </action>
  <verify>
    <automated>cd /home/toto/scm-projects/toto-castaldi.github.io && grep -q 'qualifiche.title' src/i18n/ui.ts && grep -q 'href="/qualifiche"' src/i18n/ui.ts && grep -q 'href="/en/qualifications"' src/i18n/ui.ts && npm run build</automated>
  </verify>
  <done>All qualifiche.* keys present in both it and en; section.fitness.p2 contains an internal qualifiche link in both locales; build passes (no missing-key type errors).</done>
</task>

<task type="auto">
  <name>Task 3: Render the linkified Fitness paragraph via set:html on both home pages</name>
  <files>src/pages/index.astro, src/pages/en/index.astro</files>
  <read_first>
    - src/pages/index.astro (line 44-47, the fitness Section)
    - src/pages/en/index.astro (line 27-30, the fitness Section)
    - .planning/phases/07-pagina-qualifiche-multilingua/07-PATTERNS.md (index.astro edit lines 169-185)
  </read_first>
  <action>
    In `src/pages/index.astro`, change the fitness-section line `<p>{t('section.fitness.p2')}</p>` to `<p set:html={t('section.fitness.p2')} />` so the embedded `<a>` renders instead of being escaped (mirrors the existing `set:html` on entrepreneurship.p1 / cs.p2). Leave `section.fitness.p1` as plain `<p>{t(...)}</p>`. Make the identical change in `src/pages/en/index.astro`. No other edits to these files.
  </action>
  <verify>
    <automated>cd /home/toto/scm-projects/toto-castaldi.github.io && grep -q "set:html={t('section.fitness.p2')}" src/pages/index.astro && grep -q "set:html={t('section.fitness.p2')}" src/pages/en/index.astro && npm run build && grep -q 'href="/qualifiche"' dist/index.html && grep -q 'href="/en/qualifications"' dist/en/index.html</automated>
  </verify>
  <done>Both home pages render fitness p2 via set:html; built dist/index.html contains href="/qualifiche" and dist/en/index.html contains href="/en/qualifications" (INT-01 link live).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author-controlled dictionary string → rendered HTML (set:html) | `set:html` renders raw HTML from the i18n dictionary. The content is author-authored only; there is no user input on this static site. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering (XSS) | set:html on section.fitness.p2 and qualifiche strings | accept | Strings are author-controlled with no user input on a static site (RESEARCH Security Domain). Keep set:html content authored-only; do not interpolate any external/user data into these strings. |
| T-07-05 | Information Disclosure | incorrect hreflang regressing home pages | mitigate | `alternates` defaults reproduce locale-root output; build-time grep asserts home hreflang still points to roots (Task 1 verify). |
| — | PII / data leakage | N/A | accept | This plan handles no PII; redaction is Plan 01's concern. |
| — | Spoofing / Repudiation / DoS / Elevation | N/A | accept | Static, no-backend, no-auth, no-input site; no package installs. Not applicable. |
</threat_model>

<verification>
- `npm run build` passes after each task (catches missing keys / type errors / broken refs).
- `grep 'hreflang="en"' dist/index.html | grep -q 'github.io/en/'` — home-page hreflang unchanged (INT-03 no regression).
- `grep -q 'qualifiche.title' src/i18n/ui.ts` and keys present in both it and en (INT-02).
- `dist/index.html` contains `href="/qualifiche"`; `dist/en/index.html` contains `href="/en/qualifications"` (INT-01).
</verification>

<success_criteria>
Base.astro is path-aware via an optional `alternates` prop (home pages byte-identical); all qualifiche.* keys exist in both locales; the Fitness paragraph links into the qualifiche page in both IT and EN and renders correctly. Build green.
</success_criteria>

<output>
Create `.planning/phases/07-pagina-qualifiche-multilingua/07-02-SUMMARY.md` when done. Record the exact `alternates` prop shape and the final list of qualifiche.* keys — Plan 03 consumes both.
</output>
